import Section, { SectionTitle } from "@/components/ui/section";

export default function PartnerBuisnessSection() {
  return (
    <Section className="bg-teal" layer={5}>
      <SectionTitle>Partner Buisness</SectionTitle>

    </Section>
  );
}