servers:
  web:
    - 104.131.185.135

env:
  secret:
    - ReSendKey

proxy:
  app_port: 3000
  host: faithbranch.com
  ssl: true
  healthcheck:
    path: /up
  traefik_extra_configs:
    - "http.routers.accounting_app.rule=Host(`accounting.faithbranch.com`)"
    - "http.routers.accounting_app.service=accounting_app"
    - "http.services.accounting_app.loadbalancer.server.port=8080"
