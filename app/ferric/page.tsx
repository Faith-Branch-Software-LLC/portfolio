import FerricPage from '@/components/app/ferric/FerricPage';

export const metadata = {
  title: 'Ferric — Cassette Player for iPhone',
  description:
    'Create virtual cassette tapes, add tracks from your music library, decorate with a drawing editor, and play music through an animated cassette UI.',
};

export default function Page() {
  return <FerricPage />;
}
